#!/usr/bin/env zsh -i

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

uv run autowiki lint-frontmatter --staged
uv run autowiki generate-index --repo-root "$repo_root"
git add index.md
